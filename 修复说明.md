# 填答链接"链接无效"修复补丁（v1.2.2）

## 问题根因（已排查确认）

你的线上链接 `https://wj.skyzyf335.top/s/wvh14x425e` 打开提示"链接无效"，
**不是代码 bug，而是部署配置问题**：

线上实际行为（用 curl 抓包确认）：

| 路径 | 线上实际返回 | 正常应返回 |
|---|---|---|
| `/s/wvh14x425e` | **308 重定向 → `/fill`**（问卷 ID 丢失） | 200，保持 URL 加载填答页 |
| `/fill.html` | 308 重定向 → `/fill` | 200 |
| `/abc`（任意路径） | 200（SPA fallback） | - |
| `/api/surveys/wvh14x425e/public` | 200（问卷数据正常） | 200 |

也就是说：你线上部署的配置把 `/s/*` 做成了 **308 重定向到 `/fill`**，
浏览器跳转后地址栏变成 `/fill`，填答页脚本里就再也找不到问卷 ID，于是提示"链接无效"。
问卷本身、API、数据库都是好的（数据正常返回）。

## 需要修改的 3 处

### ① 删除 Cloudflare 面板上的 308 重定向规则（最关键）

打开 Cloudflare 控制台 → 找到 `wj.skyzyf335.top` 所在的账户：

- **Rules → Redirect Rules**：删除"把 `/s/*`（或全部路径）重定向到 `/fill`"的规则；
- **Rules → Bulk Redirects**：如有类似规则一并删除；
- 如果域名是通过 Worker 转发的，检查 Worker 里是否写了 `return Response.redirect('/fill')` 之类的代码。

> 凡是"把 /s/xxx 跳到 /fill"的配置都要删掉。`_redirects` 文件的 200 重写（保持 URL）才是正确做法。

### ② 确认线上 `_redirects` 文件内容

本补丁已附带正确的 `public/_redirects`，内容必须**只有这一行**：

```
/s/*  /fill.html  200
```

（`200` 表示"URL 不变、内部换成 fill.html"，这才是正确的；如果线上是 `308` 或重定向到 `/fill`，就是它导致的。）

### ③ 替换 `public/fill.js`（防御加固）

新版本增强了两点：

1. **ID 解析更健壮**：支持标准 `/s/问卷ID`、带参数 `?id=问卷ID`、`#/s/问卷ID` 三种形态；
2. **失败提示更友好**：万一再遇到链接异常，会显示当前打开的完整地址，方便一眼看出问题。

## 部署步骤

1. 把补丁包里的 `public/fill.js`、`public/_redirects` 覆盖到你 GitHub 仓库对应位置；
2. `git add -A && git commit -m "fix: 填答链接重定向丢 ID 问题" && git push`（CF 会自动重新部署）；
3. 等部署完成后，**先验证重定向已消失**：

```bash
curl -sI https://wj.skyzyf335.top/s/wvh14x425e | head -3
```

- 修复前：返回 `308` + `location: /fill`；
- 修复后：返回 `200`，无 location 头。

4. 浏览器打开 `https://wj.skyzyf335.top/s/wvh14x425e`，应能正常看到问卷并可填写。

## 如果验证后仍是 308

说明 308 来自面板规则（① 没删干净）或 Worker 转发，代码层面已无法绕过——
因为 308 跳转后浏览器地址栏里已经没有任何问卷 ID 信息。
请再检查一次 ① 中列出的所有位置，直到 `curl` 返回 200。
