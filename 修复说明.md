# 填答链接"链接无效"修复方案（v1.2.2）

## 问题根因（已线上抓包确认）

你的线上链接 `/s/问卷ID` 打开时被 **308 重定向到 `/fill`**，跳转后浏览器地址栏变成 `/fill`，
问卷 ID 随之丢失，填答页脚本找不到 ID，于是提示"链接无效"。
问卷、API、数据库本身全部正常。

实测你的线上行为（2026-09-16）：

| 路径 | 线上实际返回 |
|---|---|
| `/s/euyt2m2912` | **308 → `/fill`**（ID 丢失） |
| `/fill` | 200（填答页） |
| `/api/surveys/euyt2m2912/public` | 200（问卷数据正常） |

## 本次修复做了什么（三个层次，双保险）

### ① 新增 `functions/s/[id].js`（代码根治，最关键）

Cloudflare Pages 的请求处理优先级是 **Functions > _redirects > 静态资源**。
新增的路由让 `/s/问卷ID` 直接由 Function 以 200 返回填答页、URL 保持不变，
**即使线上仍有 308 重定向规则，也不会再触发跳转**，问卷 ID 不会再丢。

### ② `public/_redirects` 保持正确写法

```
/s/*  /fill.html  200
```

`200` 表示"URL 不变、内部换成 fill.html"；如果你的线上是 `308` 或跳到 `/fill`，请改回这一行。

### ③ `public/fill.js` 解析增强

支持标准 `/s/问卷ID`、`?id=问卷ID`、`#/s/问卷ID` 三种形态；链接异常时会显示当前地址便于排查。

## 部署步骤

1. 用本压缩包覆盖你 GitHub 仓库（新增 `functions/s/[id].js`，更新 `public/fill.js`、`public/_redirects`）；
2. 提交并推送，等 CF 自动部署完成（约 1-2 分钟）；
3. 验证：

```bash
curl -sI https://wj.skyzyf335.top/s/euyt2m2912 | head -3
```

- 修复后应返回 `200`，**没有** `location:` 跳转头；
- 浏览器打开 `https://wj.skyzyf335.top/s/euyt2m2912` 应直接看到问卷并正常填写。

## 万一部署后仍是 308

只有一种可能：你的 308 规则配在了 **Cloudflare 面板**（域名级，优先于 Pages 内的一切），
此时请到 Cloudflare 控制台删除：

- **Rules → Redirect Rules**（删掉跳转到 `/fill` 的规则）
- **Rules → Bulk Redirects**（如有类似规则一并删除）
- 若域名由 Worker 转发，检查 Worker 里是否有 `redirect('/fill')` 代码

**临时应急链接**（不需要改面板，部署新代码后即可用）：

```
https://wj.skyzyf335.top/fill?id=euyt2m2912
```
