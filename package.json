{
  "name": "survey-workbench",
  "version": "1.0.0",
  "private": true,
  "description": "问卷工作台：问卷创建 / 管理 / 回收 / 统计 / AI 生成与分析的 Cloudflare Pages 全栈应用",
  "type": "module",
  "scripts": {
    "dev": "node dev-server.js",
    "deploy": "wrangler pages deploy",
    "db:init": "wrangler d1 execute survey-workbench --remote --file=schema.sql",
    "secret:deepseek": "wrangler pages secret put DEEPSEEK_API_KEY",
    "secret:admin": "wrangler pages secret put ADMIN_PASSWORD",
    "check": "node --check public/app.js && node --check public/fill.js && node --check public/renderer.js && for f in functions/_lib/*.js functions/api/**/*.js functions/api/**/**/*.js functions/_middleware.js; do node --check \"$f\" || exit 1; done && echo 'all syntax OK'"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
